name: compile-code

on: [push, pull_request]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: check it out
        run: |
          ls
          cd Lotto
          ls
      - name: Run the build script
        run: |
          cd Lotto
          make
          ./lotto

